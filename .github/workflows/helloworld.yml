name: "Hello World"

on: [push]

jobs:
  sample:
    name: Hello World Job
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello World"
      - run: echo "The job is now complete"
      - run: echo "Goodbye"


